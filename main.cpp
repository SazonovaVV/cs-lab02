#include <iostream>

using namespace std;

int main()
{
    cout << "Enter A and B: ";
double a, b, max;
cin >> a >> b;
cout << "A + B = " << a + b << '\n';
cout << "A - B = " << a - b << '\n';
cout << "A * B = " << a * b << '\n';
cout << "A / B = " << a / b << '\n';
if(A>B) max=A; else max=B; cout<<max;
    return 0;
}
